[package]
name = "ochams-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Compiler core for source-stored architecture graphs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
winnow = "1.0.3"

[dev-dependencies]
ochams-fixtures = { path = "../ochams-fixtures" }
